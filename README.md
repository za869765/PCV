# 佳里區衛生所 - 疫苗掛號對針統計系統

Google Apps Script（GAS）Web App 單檔專案。程式碼實際部署在 script.google.com，
本 repo 為代碼備份與版本管理（教訓：v2 舊碼曾因只存在 GAS 而差點遺失）。

## 檔案

| 檔案 | 說明 |
|---|---|
| `Code.gs` | GAS 後端（現行版） |
| `index.html` | GAS 前端（現行版） |
| `legacy/Code.gs` | v2 舊版（四檔比對：JN + PHIS 6Z/6k/6V），v3.0 移除 PHIS 前的原始碼 |
| `legacy/Index.html` | v2 舊版前端 |

## 版本沿革

- **v2.x**（legacy/）：四檔比對模式。新冠＝JN∪6k、流感＝6Z、肺鏈＝6V，含流感 Q 欄 F 分類
- **v3.0**：移除 PHIS 驗證，僅剩 NIIS 名單統計；CSV 單迴圈解析
- **v3.1**：浮標加出生日期、配色調整
- **v3.2**：疫苗辨識設定驅動（Script Properties 全所共用）、齒輪設定面板、代號計分制
- **v3.3～v3.4**：前端動畫／浮標／設定面板 UI 打磨
- **v3.5**：PHIS 交叉對針回歸（6K/6Z/6V 選傳，NIIS 為準列差異名單）＋
  流感身份別 F 分類統計（附件14 新標準對照表，未知代碼動態顯示）
- **v3.6**：系統更名 PHIS → HIS（顯示文字全改）；取消對針差異比對，改「加計模式」——
  HIS 檔中 NIIS 名單沒有的人直接加進針數與名單群組，名字旁備註 (HIS)、浮標顯示來源；
  加計群組由「診斷代號/疫苗劑別」比對辨識代號（限同類別），辨識不到用該類別第一組；
  6Z 檔「NIIS身分別」欄一併帶入流感 F 分類
- **v3.7**：產 NIIS 匯入檔——HIS 檔中 NIIS 名單沒有的人依媒體資料上傳範本（24 欄）
  產出 Big5 CSV 直接匯入 NIIS。身分別自動填（新冠預設 C11、流感另設；檔內值優先；肺鏈不填）；
  針劑（疫苗種類＋批號）可上傳 NIIS「結存量」.xls 解析匯入（SheetJS CDN 延遲載入），
  自動依疫苗辨識設定歸類、存 Script Properties 記憶，下次直接下拉選（批次拉選）；
  接種日期預設＝今天或過去最近的禮拜四（民國 7 碼）；劑別優先序：HIS 檔值 > 槽預設 > 1；
  出生日期去民國前置 0（0480327→480327，比照實際匯入成功案例）
- **v3.8**：新冠身分別改依「接種對象年齡」自動判斷（只看出生年：接種年−出生年）——
  6月-4歲 C15／5-11 C14／12-17 C17／18-49 C12／50-64 C10／65-74 C11／75+ C08A；
  HIS 檔缺出生日期時才用「新冠備援代號」；流感仍用設定代號、肺鏈不填
- **v3.9**：流感身分別個別拉選——產檔面板列出 6Z 名單，每人可從附件14 對象別
  代碼表（F01~F09 全 19 碼）下拉選；優先序：個別拉選 > 檔內值 > 流感預設代號
- **v4.1**：HIS 加計者名單標示定案＝名字底線＋紅色粗體＊（樣式四選一討論定案）＋圖例
- **v4.2**：產檔面板大改版——全螢幕四大容器（共用/新冠紅/流感藍/肺鏈橘）；
  結存量檔改虛線夾帶區（夾帶即自動解析＋動畫回饋）；機構代碼僅檢視；
  接種日期改日期選擇器（即時民國換算，預設上個週四、週四當天押當天）；
  齒輪「身分別自動判讀設定」（新冠年齡對照表 7 區間代碼、流感所內 F03A/外設 F03B、
  公費門檻：特別月份預設 10 月＝65 歲、其他月份＝50 歲，皆可設定存雲端）；
  流感容器：外設站切換、名單勾選＋批次套用（全選/只選不符）、年齡不符門檻紅底＋
  浮標原因（未拉選例外前後端皆擋產檔）、NIIS 流感已有者灰階自動略過
  （後端 getNiisFluIds 依正式辨識設定判定）；「特殊身分別清單」列印
  （可依姓名筆劃/性別/身分別排序、大勾選框供核對特殊憑證留存、簽章欄）
- **v4.4**：主頁名單 HIS 加計流感者不符門檻未拉選→紅色脈動＋⚠ 醒目動畫
  （data-his-flu 流感專屬標記，設定/拉選即時消長）
- **v4.5**：匯出面板改橫式全螢幕 2×2 四宮格（檔案共用/新冠/流感/肺鏈各一角，
  未夾帶顯示佔位）；針劑分配到人——三種疫苗名單每人可個別拉選針劑
  （同日最多五種批號分不同人；未拉選用主選；勾選＋批次套用；缺針劑擋下列名）；
  特殊身分別清單移除簽章列（三處）
- **v5.6**：針劑檔解析通吃「庫存量查詢」.xlsx（批號/有效日期/庫存總量(劑) 欄別名＋
  疫苗名稱去中文括號尾；辨識不出三類別的疫苗（BCG/MMR…）略過不存並回報筆數、
  舊污染重匯即清；上限與儲存端一致 100）；針劑匯入獨立——「身分別設定與匯出」
  按鈕常駐、開面板不再要求先夾 HIS 檔（無名單也能先匯針劑）
- **v5.5**：手動新增輸入框移除 placeholder（原全顯新冠範例造成混淆；候選仍由同類 datalist 提供）
- **v5.4**：主頁 HIS 卡加高；手動新增記憶候選依疫苗類別隔離（新冠>新冠、流感>流感、肺鏈>肺鏈，代號與批號皆是）
- **v5.3**：流感表頭元件互換——「目前設定是＋切換」靠左、「⚙判讀設定」與新冠一致靠最右
- **v5.2**：可點擊表頭藍漸層底＋👆手勢引導（未夾帶灰底無手勢，與不可點區域明顯區別）；⚙判讀設定回歸各自容器（新冠格=年齡對照表、流感格=門檻/代號/F代碼表、肺鏈不設；分域儲存互不覆寫）
- **v5.1**：疫苗代碼字元防呆——官方種類正典表（含 115/10/1 新增 FluAdj/FluHD）匯出自動校正大小寫；辨識/結存量匯入/手動新增/匯出四處清 BOM/零寬/nbsp/全形空白
- **v4.9**：聚焦時未展開標題列壓暗底（滑過提亮）＋聚焦格紫描邊，明顯區分工作區
- **v4.8**：展開改聚焦模式——任一格展開時其他格縮成只剩標題列、展開格吃滿剩餘全螢幕；全收合維持 2×2（有效利用 UI）
- **v4.7**：四大格固定 2×2 吃滿整頁高度（就這四格），內容在格內展開、格內捲動
- **v4.6**：修掉 exportModal 行內 max-width:860px（真全螢幕）；四宮格改手風琴——預設全縮小成標題＋摘要，點擊展開整列寬（一次一格；純 class 切換不重繪，輸入狀態不流失）；面板更名「身分別設定與匯出」
- **v4.3**：十三點 UI/流程優化——HIS 卡片移除 ✕（重新夾帶即替換）；按鈕更名
  「身分別設定與匯出」；主頁名單浮標顯示「目前身分別＋來源」（前端依設定即時計算，
  雲端設定載入前用內建預設 fallback）；判讀設定面板補流感 F 代碼表（可編輯/增刪，
  名單拉選選單同步使用）；紅底不符列脈動＋👉手勢動畫；場次顯示「目前設定是：所內/所外」
  ＋「切換」鈕；三容器手動新增 datalist 記憶自動填；針數調整框 readonly＋去 spinner；
  一鍵列印：隱藏調整鍵、排版美化、資料量自適應行距（多→窄+長段落標「續下一頁」、
  少→寬）、附「流感特殊身分別檢核表」獨立一頁；寄給承辦：原始檔（檔名註明）＋
  已設定身分別的 NIIS 匯入檔＋特殊身分別清單 PDF 一併附上（產不出時備註原因照寄）

## HIS 檔案格式備註（2026-08 實測）

HIS（舊稱 PHIS）掛號匯出為 13 欄：`掛號序號,姓名,出生日期,身份證,電話1,電話2,行動電話,性別,優待別,診斷代號,疫苗劑別,NIIS身分別,通訊地址`
— 身分證在第 4 欄、「優待別」欄直接標 6V/6k/6Z（可自動驗證檔案類型）。
舊版 legacy 代碼假設 A欄=身分證、B欄=姓名，與此格式不符；v3.5 起以表頭偵測通吃新舊格式。

NIIS 匯入檔 17 欄，第 11 欄=疫苗種類、第 17 欄=身份別（流感 F 代碼）。
媒體資料上傳範本為 24 欄，前 17 欄位置相同，解析相容。

## 部署

1. 開 script.google.com 該專案，貼上 `Code.gs` 與 `index.html`
2. 部署 → 管理部署 → 編輯 → 新版本 → 部署
